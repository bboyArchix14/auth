server:
  port: 7071
spring:
  application:
    name: ms-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bboyArchix14/auth.git # ruta del repositorio git
          searchPaths: config-data
          default-label: Main
  security:
    user:
      name: root
      password: 123456